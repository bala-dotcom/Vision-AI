# Production Environment Variables for Backend (Hostinger)
# Copy this to .env on your Hostinger server and update with your actual values

# Database Configuration (Hostinger)
DB_HOST=localhost
DB_USER=u623025070_vision2025
DB_PASSWORD=Vision@20252025
DB_NAME=u623025070_vision_ai_db
DB_PORT=3306

# Server Configuration
PORT=3001
NODE_ENV=production

# Frontend/Backend URLs (Update with your actual production domains)
FRONTEND_URL=https://vision.innovfix.in
BACKEND_URL=https://api.vision.innovfix.in

# Google Cloud Configuration
# Add your Google Cloud service account credentials here
# GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project.iam.gserviceaccount.com
# GOOGLE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n
# GOOGLE_PROJECT_ID=your-google-project-id
# GOOGLE_LOCATION=us-central1

# Important Notes:
# 1. Update FRONTEND_URL and BACKEND_URL with your actual production domains
# 2. If Hostinger uses a different database host (not localhost), update DB_HOST
# 3. Never commit the actual .env file to git (only .env.example)
# 4. Keep your database password secure
