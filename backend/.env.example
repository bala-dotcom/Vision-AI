# Google Cloud Service Account Configuration
# 
# To enable automatic authentication without entering tokens every time:
# 1. Create a service account in Google Cloud Console
# 2. Grant it "Vertex AI User" role
# 3. Create and download a JSON key
# 4. Copy the values from the JSON key to the variables below
# 5. Rename this file to .env (remove .example)

# Your Google Cloud Project ID
GOOGLE_PROJECT_ID=your-project-id

# Service Account Email (from the JSON key file: "client_email")
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# Private Key (from the JSON key file: "private_key")
# IMPORTANT: Keep the quotes and \n characters as-is
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here...\n-----END PRIVATE KEY-----\n"
